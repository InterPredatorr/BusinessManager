object Firebase {
    const val version = "1.11.1"
    const val bomVersion = "32.7.1"
    const val messagingVersion = "23.4.0"
    const val auth = "dev.gitlive:firebase-auth:$version"
    const val database = "dev.gitlive:firebase-database:$version"
    const val firestore = "dev.gitlive:firebase-firestore:$version"
    const val functions = "dev.gitlive:firebase-functions:$version"
    const val common = "dev.gitlive:firebase-common:$version"
    const val bom = "com.google.firebase:firebase-bom:$bomVersion"
    const val messaging = "com.google.firebase:firebase-messaging:$messagingVersion"
    const val storage = "dev.gitlive:firebase-storage:$version"
}

